{
  "name": "ezyo-forum",
  "description": "",
  "version": "0.0.1",
  "scripts": {
    "start": "godotenv -f \"./.env\" go run main.go",
    "watch": "nodemon --exec \"npm run start\" -e \"go\" --signal SIGTERM"
  }
}
